rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All requests pass through the secure Express backend using Admin SDK.
    // Deny all direct client SDK access to ensure data is only accessed via the server.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
